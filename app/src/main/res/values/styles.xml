<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- =============================== -->
    <!--        Application Theme        -->
    <!-- =============================== -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="colorPrimaryDark">@color/primary_blue_dark</item>
        <item name="colorAccent">@color/mint_400</item>

        <!-- System Bars - Transparent (respects user's system settings) -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>

        <!-- Let system decide icon colors based on background -->
        <item name="android:enforceNavigationBarContrast">true</item>
        <item name="android:enforceStatusBarContrast">true</item>

        <item name="android:windowBackground">@color/white</item>

        <!-- Default Font Family -->
        <item name="android:fontFamily">@font/pretendard</item>
        <item name="fontFamily">@font/pretendard</item>
    </style>

    <!-- =============================== -->
    <!--      TextInputLayout Style      -->
    <!-- =============================== -->
    <style name="InputLayoutStyle" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <!-- Box Colors -->
        <item name="boxBackgroundColor">@color/white</item>
        <item name="boxStrokeColor">@color/input_box_stroke_color</item>

        <!-- Box Stroke Width -->
        <item name="boxStrokeWidth">1dp</item>
        <item name="boxStrokeWidthFocused">2dp</item>

        <!-- Hint Text -->
        <item name="hintTextColor">@color/gray_700</item>
        <item name="hintTextAppearance">@style/TextAppearance.MaterialComponents.Body1</item>
        <item name="hintAnimationEnabled">true</item>

        <!-- Helper & Error Text -->
        <item name="helperTextTextColor">@color/gray_500</item>
        <item name="errorTextColor">@color/pink_400</item>

        <!-- Shape & Icons -->
        <item name="shapeAppearanceOverlay">@style/ShapeAppearanceOverlay.App.Corner.Medium</item>
        <item name="endIconTint">@color/primary_blue</item>
    </style>

    <!-- =============================== -->
    <!--      TextInputEditText Style    -->
    <!-- =============================== -->
    <style name="InputEditTextStyle">
        <item name="android:lineSpacingMultiplier">1.0</item>
        <item name="android:includeFontPadding">false</item>
        <item name="android:textColor">@color/text_primary</item>
    </style>

    <!-- =============================== -->
    <!--        Rounded Corner Shape      -->
    <!-- =============================== -->
    <style name="ShapeAppearanceOverlay.App.Corner.Medium" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">10dp</item>
    </style>

    <!-- =============================== -->
    <!--        Primary Button Style      -->
    <!-- =============================== -->
    <style name="PrimaryButton" parent="Widget.MaterialComponents.Button">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">16sp</item>
        <item name="android:fontFamily">@font/pretendard_medium</item>
        <item name="cornerRadius">8dp</item>
        <item name="backgroundTint">@color/primary_blue</item>
    </style>

    <!-- =============================== -->
    <!--         Gradient Button Style    -->
    <!-- =============================== -->
    <style name="GradientButton" parent="Widget.MaterialComponents.Button">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:fontFamily">@font/pretendard_medium</item>
        <item name="android:padding">12dp</item>
        <item name="cornerRadius">8dp</item>
    </style>

    <!-- =============================== -->
    <!--    Bottom Navigation Styles     -->
    <!-- =============================== -->
    <style name="BottomNavActiveIndicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/white</item>
    </style>

    <style name="BottomNavTextActive" parent="TextAppearance.MaterialComponents.Caption">
        <item name="android:textSize">12sp</item>
        <item name="android:fontFamily">@font/pretendard_medium</item>
    </style>

    <style name="BottomNavTextInactive" parent="TextAppearance.MaterialComponents.Caption">
        <item name="android:textSize">12sp</item>
        <item name="android:fontFamily">@font/pretendard</item>
        <item name="android:textColor">@color/white_muted</item>
    </style>

    <!-- =============================== -->
    <!--    Date & Time Picker Styles    -->
    <!-- =============================== -->
    <style name="DatePickerTheme" parent="Theme.MaterialComponents.Light.Dialog">
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="colorAccent">@color/primary_blue</item>
        <item name="colorControlActivated">@color/primary_blue</item>
        <item name="android:colorControlActivated">@color/primary_blue</item>
        <item name="android:buttonBarPositiveButtonStyle">@style/PositiveButtonStyle</item>
        <item name="android:buttonBarNegativeButtonStyle">@style/NegativeButtonStyle</item>
    </style>

    <style name="TimePickerTheme" parent="Theme.MaterialComponents.Light.Dialog">
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="colorAccent">@color/primary_blue</item>
        <item name="colorControlActivated">@color/primary_blue</item>
        <item name="android:colorControlActivated">@color/primary_blue</item>
        <item name="android:buttonBarPositiveButtonStyle">@style/PositiveButtonStyle</item>
        <item name="android:buttonBarNegativeButtonStyle">@style/NegativeButtonStyle</item>
    </style>

    <style name="PositiveButtonStyle" parent="Widget.MaterialComponents.Button.TextButton.Dialog">
        <item name="android:textColor">@color/primary_blue</item>
    </style>

    <style name="NegativeButtonStyle" parent="Widget.MaterialComponents.Button.TextButton.Dialog">
        <item name="android:textColor">@color/gray_600</item>
    </style>

    <!-- =============================== -->
    <!--      Filter Chip Style          -->
    <!-- =============================== -->
    <style name="FilterChipStyle" parent="Widget.MaterialComponents.Chip.Filter">
        <item name="chipBackgroundColor">@color/filter_chip_background</item>
        <item name="android:textColor">@color/filter_chip_text</item>
        <item name="checkedIconVisible">false</item>
        <item name="chipStrokeWidth">0dp</item>
        <item name="chipCornerRadius">16dp</item>
        <item name="chipStartPadding">12dp</item>
        <item name="chipEndPadding">12dp</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>
    </style>

    <!-- =============================== -->
    <!--      Category Chip Style        -->
    <!-- =============================== -->
    <style name="CategoryChipStyle" parent="Widget.MaterialComponents.Chip.Choice">
        <!-- Text color: muted when unchecked, dark when checked -->
        <item name="android:textColor">@color/category_chip_text_color</item>
        <item name="android:textStyle">bold</item>
        <item name="checkedIconVisible">false</item>

        <!-- Background color: light background to ensure text visibility -->
        <item name="chipBackgroundColor">@color/category_chip_background</item>

        <!-- Border: lighter when unchecked, darker when checked -->
        <item name="chipStrokeWidth">2dp</item>
        <item name="chipStrokeColor">@color/category_chip_stroke_color</item>

        <!-- Chip Shape -->
        <item name="chipCornerRadius">16dp</item>
        <item name="chipMinHeight">32dp</item>

        <!-- Chip Padding -->
        <item name="chipStartPadding">12dp</item>
        <item name="chipEndPadding">12dp</item>
        <item name="android:paddingTop">6dp</item>
        <item name="android:paddingBottom">6dp</item>

        <!-- Text Size and Elevation -->
        <item name="android:textSize">13sp</item>
        <item name="android:elevation">2dp</item>
    </style>

    <!-- =============================== -->
    <!--    Visibility Toggle Button     -->
    <!-- =============================== -->
    <style name="VisibilityToggleButtonStyle" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:textColor">@color/toggle_button_text</item>
        <item name="backgroundTint">@color/toggle_button_background</item>
        <item name="strokeColor">@color/toggle_button_stroke</item>
        <item name="strokeWidth">1dp</item>
        <item name="android:textSize">14sp</item>
        <item name="android:minHeight">48dp</item>
    </style>

    <!-- =============================== -->
    <!--      Toolbar Title Style        -->
    <!-- =============================== -->
    <style name="ToolbarTitleBold" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textStyle">bold</item>
        <item name="android:fontFamily">@font/pretendard_extrabold</item>
    </style>

    <!-- =============================== -->
    <!--      Tab Text Style             -->
    <!-- =============================== -->
    <style name="TabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">15sp</item>
        <item name="android:fontFamily">@font/pretendard_bold</item>
        <item name="textAllCaps">false</item>
    </style>

</resources>