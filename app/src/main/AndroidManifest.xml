<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ============================================================ -->
    <!-- Required Permissions -->
    <!-- ============================================================ -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Photo/Image permissions for profile picture selection -->
    <!-- For Android 13+ (API 33+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- For Android 12 and below -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- ============================================================ -->
    <!-- Package Visibility Queries (Android 11+) -->
    <!-- ============================================================ -->
    <!-- Required for Naver Login to detect Naver app -->
    <queries>
        <package android:name="com.nhn.android.search" />
    </queries>

    <application
        android:name=".ConnectMateApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/main_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/circle_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.ConnectMate"
        android:usesCleartextTraffic="false"
        tools:targetApi="31">

        <!-- ============================================================ -->
        <!-- Kakao SDK Configuration -->
        <!-- ============================================================ -->
        <!-- Kakao Login SDK requires this meta-data -->
        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="${KAKAO_APP_KEY}" />
        <!-- Note: KakaoMaps SDK v2 is initialized programmatically in ConnectMateApplication.java -->

        <!-- ============================================================ -->
        <!-- Activities -->
        <!-- ============================================================ -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.ConnectMate.Splash" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTop">
            <!-- Email Link Authentication Handler -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="connectmate.app"
                    android:pathPrefix="/__/auth/action" />
                <data
                    android:scheme="https"
                    android:host="connectmate.app"
                    android:path="/finishSignUp" />
            </intent-filter>
            <!-- Fallback for Firebase default domain -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="connect-mate-25.firebaseapp.com"
                    android:pathPrefix="/__/auth/action" />
                <data
                    android:scheme="https"
                    android:host="connect-mate-25.firebaseapp.com"
                    android:path="/finishSignUp" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SignUpActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ProfileSetupActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:label="프로필 만들기" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:launchMode="singleTop">
            <!-- App Links for connectmate.app -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="connectmate.app" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MapActivity"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:label="Map">
            <!-- App Links for map -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="connectmate.app"
                    android:path="/map" />
            </intent-filter>

            <!-- Intent filter for direct access and deep linking -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- Deep link: connectmate://map -->
                <data
                    android:scheme="connectmate"
                    android:host="map" />
            </intent-filter>

            <!-- Alternative intent filter for custom action -->
            <intent-filter>
                <action android:name="com.example.connectmate.action.VIEW_MAP" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".CreateActivityActivity"
            android:exported="false"
            android:label="Create Activity"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ActivityDetailActivity"
            android:exported="true"
            android:label="Activity Details"
            android:parentActivityName=".MainActivity">
            <!-- App Links for activity details -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="connectmate.app"
                    android:pathPrefix="/activity" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ChatRoomActivity"
            android:exported="true"
            android:label="Chat Room"
            android:windowSoftInputMode="adjustResize"
            android:parentActivityName=".MainActivity">
            <!-- App Links for chat rooms -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="connectmate.app"
                    android:pathPrefix="/chat" />
            </intent-filter>
        </activity>

        <activity
            android:name=".FriendsActivity"
            android:exported="false"
            android:label="My Friends"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".EditProfileActivity"
            android:exported="false"
            android:label="Edit Profile"
            android:windowSoftInputMode="adjustResize"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".AboutActivity"
            android:exported="false"
            android:label="앱 정보"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".OAuthWebViewActivity"
            android:exported="false"
            android:label="로그인"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <activity
            android:name="com.kakao.sdk.auth.AuthCodeHandlerActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="oauth"
                    android:scheme="kakao${KAKAO_APP_KEY}" />
            </intent-filter>
        </activity>

        <!-- Naver Login Callback Activity -->
        <activity
            android:name="com.navercorp.nid.oauth.NidOAuthLoginActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="oauth"
                    android:scheme="naverlogin" />
            </intent-filter>
        </activity>

    </application>

</manifest>
